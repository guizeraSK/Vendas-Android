<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.sk.loja">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Loja">
        <activity
            android:name=".activities.ActivityRelatorio"
            android:exported="false" />
        <activity
            android:name=".activities.ActivityListarProdutos"
            android:exported="false"
            android:label="Lista de Produtos" />
        <activity
            android:name=".activities.ActivityVenda"
            android:exported="false" />
        <activity
            android:name=".activities.ActivityProduto"
            android:exported="false"
            android:label="Cadastro de Produtos" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>